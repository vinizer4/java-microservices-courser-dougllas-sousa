spring:
  application:
    name: eurekaserver
  security:
    basic:
      enabled: true
    user:
      name: admin
      password: admin

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

logging:
  level:
    org.springframework.cloud.netflix.eureka: DEBUG